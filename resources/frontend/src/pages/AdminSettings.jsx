import { Typography } from '@mui/material';

const SettingsPage = () => {
  return (
    <Typography variant="h4" component="h1">
      Admin Settings
    </Typography>
  );
};

export default SettingsPage;